import blockNames from '../blockNames'

const FeatureItem = () => {}
FeatureItem.schema = {
  name: blockNames.FeatureItem,
  label: 'Feature items',
  getDefaultProps: () => {},
}

export default FeatureItem
